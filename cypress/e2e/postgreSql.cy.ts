/// <reference types="cypress" />


describe("PostgreSql page", () => {

    
    cy.visit("http://localhost:3000/postgresql");

    it("should open the create form", () => {
        // cy.get('button').contains('Create').click();
    });
  
    
  });